# Deployment

- The LGTM stack is deployed using Docker Compose for simplified setup and management.
- The server it is deployed to should have Docker and Docker Compose installed.
- The server can be accessed via SSH for deployment and management. The server details are in the `.env` file.

